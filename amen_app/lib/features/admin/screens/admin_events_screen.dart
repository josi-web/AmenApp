import 'package:flutter/material.dart';

class AdminEventsScreen extends StatelessWidget {
  const AdminEventsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Events Management Screen'),
    );
  }
}
